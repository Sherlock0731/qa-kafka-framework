# JUnit Platform Configuration

# Parallel execution DISABLED by default (enable with -Dthread.count > 1)
junit.jupiter.execution.parallel.enabled = false
junit.jupiter.execution.parallel.mode.default = same_thread
junit.jupiter.execution.parallel.mode.classes.default = same_thread

# Fixed strategy configuration (used when enabled via systemPropertyVariables)
junit.jupiter.execution.parallel.config.strategy = fixed
junit.jupiter.execution.parallel.config.fixed.parallelism = 1
junit.jupiter.execution.parallel.config.fixed.saturate = false
